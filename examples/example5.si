val make_obs = fun(obs, x_pre) ->
    let  x <- gaussian(x_pre, 1) in
    let () = observe(gaussian(x,1),obs) in
    x
in

let data = List.range(1, 3) in

let  x_init <- gaussian(0,100) in
let xs = fold(make_obs,data,x_init) in
xs